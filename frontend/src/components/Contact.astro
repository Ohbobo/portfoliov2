---
import { res } from '../data/api';
---

<h1>test</h1>
<ul>
    {res.map((res: {
[x: string]: unknown; title: string; 
}) => (
        <li>
            <p>{res.title}</p>
            <span>{res.id}</span>
        </li>
    ))}
</ul>

